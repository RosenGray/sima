"use client";
import { Container, Flex } from "@radix-ui/themes";
import { styled } from "styled-components";

export const AuthLayoutContainer = styled(Container)`

`;
export const AuthLayoutFlex = styled(Flex)`
  height: 100vh;
`;

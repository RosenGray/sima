"use client";
import { Container } from "@radix-ui/themes";
import { styled } from "styled-components";

export const PublishAdJobsPageContainer = styled(Container)`
  padding: var(--space-4);
  max-width: 1200px;
  margin: 0 auto;
`;

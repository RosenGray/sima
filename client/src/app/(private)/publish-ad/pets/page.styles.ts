"use client";
import styled from "styled-components";
import { Container, Grid } from "@radix-ui/themes";

export const PetsPageContainer = styled(Container)``;

export const PetsGrid = styled(Grid)``;

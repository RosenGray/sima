"use client";
import { Box } from "@radix-ui/themes";
import { styled } from "styled-components";

export const PublishAdVehiclesPageContainer = styled(Box)`
  width: 100%;
  max-width: 1080px;
  margin: 0 auto;
  padding: var(--space-4);
`;

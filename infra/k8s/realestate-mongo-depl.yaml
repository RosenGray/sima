apiVersion: apps/v1
kind: Deployment
metadata:
  name: realestate-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: realestate-mongo
  template:
    metadata:
      labels:
        app: realestate-mongo
    spec:
      containers:
        - name: realestate-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: realestate-mongo-srv
spec:
  selector:
    app: realestate-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
